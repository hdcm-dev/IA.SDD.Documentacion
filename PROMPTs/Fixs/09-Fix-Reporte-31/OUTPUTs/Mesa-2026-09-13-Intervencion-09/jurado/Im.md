JUEZ: Im

| Ítem | Voto | Fundamento |
|---|---|---|
| J9-01 | PROCEDE | Si no se adopta la figura, pierde el Product Owner: su pedido (`ev-07`) queda sin casa. También el orquestador, que ya expedientó dos casos (0001 del framework y EXP-0001 de `Lab-Geometria`) sin forma que los rija. Si se adopta, a los destinos no se les rompe nada, porque rige sólo hacia adelante (plan, cambio 1). El costo de la regla lo acotan J9-04, J9-09 y J9-12. |
| J9-02 | PROCEDE | Con la opción A, todo destino que migre cruza un major y el bloque de impacto queda vacío. Además, las citas D9 `humano` apoyadas en chat son 0 en los dos destinos (M9-REF-05, E1). Así que A cobra el costo a todos y a nadie le arregla nada. C no vuelve incumplido nada ya escrito y es coherente con la decisión tomada 1. |
| J9-03 | PROCEDE | Con cinco dígitos, `IA.SDD` y `Lab-Geometria` quedan con A1 en rojo para siempre y con dos formas de nombre vivas. Tampoco se sabe si después de `0001` viene `00001` o `00002` (M9-VER-03, M9-REF-07). Con cuatro dígitos, las dos carpetas existentes quedan conformes en nombre y a nadie se le exige renombrar. |
| J9-04 | PROCEDE | Con «verbatim como folio», un caso con mesa en un destino pasa a unos 16 folios y 127 campos, y además cae en el anti-patrón de §7 de la propia regla (M9-REF-01). Lo pagan el orquestador y quien lee. Queda un riesgo para quien sigue el enlace: después de un squash, la cita apunta a un commit inalcanzable, cosa que ya pasó en el 0001 (M9-TRZ-04). Ver variante. |
| J9-05 | PROCEDE | Sin precedencia, dos orquestadores deciden distinto sobre el mismo caso (M9-FOR-03). «Ante la duda, no se abre» deja sin expediente los casos dudosos, que son los que el Product Owner quiere tener registrados (M9-REQ-05). El costo de abrir de más es una carpeta. El de no abrir es perder la trazabilidad, y eso no se recupera después. |
| J9-06 | PROCEDE | Con «tipo del último folio a secas», una errata después de la resolución reabre el expediente en la reanudación (M9-FOR-01). Eso genera pendientes falsos para quien reanuda y para el Product Owner que lee R1. La derivación sobre la secuencia no le cambia nada a ningún expediente de forma vigente, porque todavía no hay ninguno. |
| J9-07 | PROCEDE | Con «después de la fusión», un folio ya empujado se puede reescribir legalmente mientras A7 lo marca, así que letra y comando se contradicen (M9-REQ-04). Sin excepción, el 0001 no podría recibir la constancia S2 de la decisión tomada 4. Hay que dejar dicho que la redacción posterior al push no retira nada: los originales siguen accesibles por SHA (M9-SEG-01). |
| J9-08 | PROCEDE | Con el S2 del borrador, una credencial sellada se redacta en vez de revocarse. Además, su comando no detecta la fuga real del 0001: marca 7 falsos positivos y no encuentra ninguno de los 4 casos verdaderos (M9-SEG-02, M9-SEG-06). Quien sufre el daño es un tercero, y ese daño no se deshace. Distinguir la visibilidad con sonda o con declaración fechada evita ofuscar de más en `Lab-Geometria` (decisión tomada 3). Cuesta 40 ms por push. |
| J9-09 | PROCEDE | Aplicada «donde se asiente», sin límite, deja incumplidas desde el primer día 127 líneas de los dos destinos (M9-REF-04). Eso sería un hallazgo en cada auditoría de migración. Además, quien escribe `Decisiones-Pendientes.md` no lee esta regla, así que la obligación no le llegaría. Acotarla a §7.0 y §7.1 y hacerla no retroactiva no rompe nada ya escrito. |
| J9-10 | PROCEDE | Con una sola vía directa, 13 de los 31 artefactos del EXP-0001 no citan el expediente y quedan fuera de la derivación (M9-TRZ-03). Lo pierde quien reconstruye el caso. Admitir dos saltos a través de `Audit/` no exige reescribir ningún artefacto de destino, y el punto interno evita que A10 marque como rota una carpeta que existe (M9-TRZ-02). |
| J9-11 | PROCEDE | Con cinco pasos numerados, la mesa puede devolver la misma detención que la abrió (M9-FOR-08), y M4 no puede decidir qué es un «lote» (M9-LEC-06). Quien lo paga es el Product Owner, al que se le vuelve a preguntar. Con «no reingresa» y el corte de M4 queda una sola escalada por tropiezo. Cablear la reanudación cierra la omisión de M9-REQ-06. |
| J9-12 | PROCEDE | La alternativa deja el EXP-0001 de `Lab-Geometria` como abierto en cada reanudación, para siempre. Cerrarlo exigiría escribir en un destino, que esta mesa no puede hacer (M9-REF-06, E1). El pase `Cierra con:` evita que se acumulen pendientes falsos, uno por cada resolución. |
| J9-13 | PROCEDE | Sin `Objeto`, el listado de `Expedientes/` y el R1 de la reanudación no dicen de qué trata cada caso. Eso afecta al lector sin contexto y al Product Owner, que pidió explícitamente un README «que describa» (M9-REQ-03). El impacto de sumarlo es un campo fijo que no se desincroniza, y A2 atrapa la divergencia de Número y Título (M9-TRZ-05). |
| J9-14 | PROCEDE | Los tres actos que ya fallaron en el 0001 son S2 tardía, cartas tardías y doble despacho. En la alternativa quedan enterrados en 186 palabras de prosa (M9-REF-02), y el daño vuelve en el próximo caso con mesa. Los tres momentos de uso no suman texto y dejan cada procedimiento por debajo de nueve pasos. |
| J9-15 | PROCEDE | Con 18 cambios hay 18 filas y cabeceras, y además una segunda copia del árbol en `Conocimiento/` que hay que mantener sincronizada (M9-REF-08). La remisión en la 13.17 corrige lo único que choca, la oración E4. Dejar `Deriva-Rules.md` intacto es coherente con J9-02, y el `Catalogo-De-Criterios.md` evita que falle la comprobación 12. |
| J9-16 | PROCEDE | Editar el README del 0001 después de la fusión lo pondría a chocar con A7 (M9-VER, pregunta 5), así que los dos folios cierran el caso sin tocar lo sellado. La 019 tiene que decir toda la verdad: el squash no retiró los originales (M9-SEG-01). Si no lo dice, el lector cree que la exposición terminó. |
| J9-17 | PROCEDE | Postergarlo deja la IP publicada en `Examples/` vigente, que es lo que un lector copia. Lo que queda en `_legacy/` es una IP de red privada, no alcanzable desde Internet, y ahí el impacto es bajo. No tocar esa carpeta preserva su intocabilidad. Declararlo en el `CHANGELOG` evita que un destino lo tome por una regresión. |

VETO: ninguno

VARIANTES:
- J9-04: la constancia `ruta@commit` cita un commit alcanzable desde la rama principal. Si hubo squash, se vuelve a foliar al commit de squash (M9-TRZ-04).
- J9-07 y J9-16: la constancia S2 posterior al push declara que la redacción no retira los originales, que siguen accesibles por SHA y por el PR, y que pedir el retiro es decisión del Product Owner.
