**Comisión: Trazabilidad documental (AG-00110)**, postergada por cupo en el ciclo anterior (deuda D-6; el motivo del descarte original —«no hay corpus de expedientes con enlaces que medir»— se corrigió en el folio 013 A.3: sí lo hay). Siglas: `M9-TRZ-NN`.

**Tu competencia:** identificadores, citas y enlaces entre expedientes, artefactos, `Audit/` y repositorios; que una cita resuelva y sea única; la dirección expediente → artefacto (declarada) y artefacto → expediente (derivada). **No te corresponde** seguridad, ni la lógica de los conjuntos, ni la conveniencia procesal.

**Tu única pregunta:** ¿el sistema de citas de `Expediente-Rules.md` (§3.1, §5, A10) resuelve en los dos sentidos sin una segunda declaración, y sin colisionar con lo que ya vive en los destinos? En particular:
1. **Colisión, medida.** `EXP-0001` de cuatro dígitos vive en `Lab-Geometria` `main`: medilo (`git -C <workspace>/PROG2/Geometria/Lab-Geometria grep -c 'EXP-' main | wc -l`; ocurrencias con `-o`). La regla decide **no acuñar `EXP-`** y numerar `NNNNN-<Titulo>` local sin prefijo, y nombrar la evidencia `NNN-<slug>`. ¿Es la decisión correcta contra ese corpus, o hay una colisión que la regla no ve? Medí también `EV-` en `RPI.VideoControl` y `Lab-Geometria`, y `ev-NN`.
2. **La cita local** `SDD/Expedientes/00002-<Titulo>/` con el folio en prosa, y **la cita calificada** `<repositorio>/SDD/Expedientes/00002-…`. ¿A10 (`git grep -ohE '(^|[^/A-Za-z0-9.-])(SDD/)?Expedientes/[0-9]{5}-…'`) distingue bien las dos? Construí citas que lo engañen: una ruta relativa `../../Expedientes/00002-X`, una cita dentro de un bloque de código, una cita con acento en el título, una cita a un expediente de cuatro dígitos (forma histórica).
3. **La única vía declarada**: la fila de control de cambios del artefacto nombra la carpeta. ¿Con eso `git grep -l 'Expedientes/00002-'` deriva todos los artefactos que el caso cambió? ¿Qué se pierde frente a la tabla «Artefactos de especificación tocados» que `Lab-Geometria` escribió a mano en su README (`git -C <workspace>/PROG2/Geometria/Lab-Geometria show main:SDD/Expedientes/0001-Migracion-Normativa-A-13.16/README.md`, §3)? ¿Esa tabla está completa contra `git log`? Medilo.
4. **`Root-Rules.md` §9.2 y §9.5**: el plan excluye el número de expediente y el folio como familias excluidas con motivo (como `FA-NN`). ¿Es coherente con §9.1 («una familia excluida no toma ámbito») que el número sea «local al repositorio»? ¿Qué pasa con `Root-Rules.md` §10 R5 («la identidad de una referencia es el identificador del destino… nunca solo la ruta») cuando la cita al expediente es sólo una ruta?
5. **Folia por enlace** (§5): una constancia con `ruta@commit` al registro de mesa. ¿Qué forma exacta necesita para que resuelva después de una migración que archiva el registro a `_legacy/` del destino? ¿Y la cita inversa desde `Mesa-<fecha>.md` hacia el expediente?
6. **La 13.17** (`Conocimiento/Knowledge-Mesa-De-Expertos-A-Pedido.md`): con el cambio 13 del plan, ¿queda alguna referencia a `00-Contrato-De-Entrada`, `NN-Informe-`, `NN-Plan-Y-Cierre` o «evidencia E4» en el conjunto normativo o en `Conocimiento/`? Medilo con `git grep` sobre `main` y decí qué tiene que barrer la intervención (incluida `SDD/Devs/Guides/Coherencia-Mesa-De-Expertos-A-Pedido.md`).
