**Comisión: Verificación (N2, núcleo permanente)**. Siglas: `M9-VER-NN`. En el ciclo anterior (folio 010) propusiste C1–C15 e I1–I7; el dictamen los redujo a A1–A10 e I1–I4.

**Tu única pregunta:** ¿los criterios de `Expediente-Rules.md` §6, con sus comandos tal como están escritos en la regla, verifican la forma mínima, y el criterio decisivo del reporte 31 §7 (1) se cumple? En particular:
1. Corré `bash <scratchpad>/a1a10.sh <scratchpad>/caso SDD/Expedientes/00001-Enlace-Roto-En-Roadmap` (el caso de prueba de dos folios, en un repositorio git de prueba) y verificá que cada comando de `a1a10.sh` es **idéntico** al de la regla §6 (diff a ojo, o extraelos con `grep -oE '`[^`]+`' plan/Expediente-Rules.md`). Toda diferencia es hallazgo.
2. **Probá cada criterio fallando**, en una copia de `<scratchpad>/caso/` bajo tu propio directorio temporal (no modifiques `<scratchpad>/caso/`): un folio con salto, un tipo fuera del conjunto, un `Corrige` a un folio inexistente, un pase ausente, un testimonio con la huella cambiada, una evidencia sin sus tres líneas, una cita a un expediente que no existe, un borrado en `evidencia/` después de la fusión (criterio 4 del reporte 31 §7). Un criterio que no falla cuando debe es hallazgo P1.
3. **A7**: `m=$(git log --first-parent --format=%h --diff-filter=A main -- "$X/README.md" | tail -1)` toma como «fusión» el commit de primera línea donde apareció el README. Probalo con una fusión por merge commit y con una por squash; probalo cuando el expediente se creó directamente en `main` sin rama. ¿Qué pasa si el README se renombró alguna vez? ¿Y si la rama principal no se llama `main`?
4. **A10** sobre el framework real: corré `cd <workspace>/IA/SDD/IA.SDD-i09 && git grep -ohE '(^|[^/A-Za-z0-9.-])(SDD/)?Expedientes/[0-9]{5}-[A-Za-z0-9-]+' main -- SDD Expedientes` y decí qué devuelve; después de aplicar el plan, la regla misma va a contener rutas ilustrativas como `SDD/Expedientes/00002-<Titulo>/`: ¿A10 las va a marcar como no resueltas? ¿Cómo tiene que estar escrita la regla o el criterio para que no?
5. Sobre los dos expedientes reales (`<scratchpad>/ev/ev-03-a1a10-reales.out`): la regla los declara forma histórica y no les exige A1–A10. ¿Está bien dicho en la regla (§5.1) de modo que un auditor no los cuente como hallazgo? ¿Y `_legacy/<N>/` no los contiene (`ls <workspace>/IA/SDD/IA.SDD-i09/_legacy/13.17`)?
6. La comprobación de la Parte V de la guía (`find SDD -type f -not -name '*.md'` → 0) no alcanza a `Expedientes/`, que trae `.sh`, `.out` y `.txt`. ¿La regla lo declara? ¿Qué comprobación cubre el hueco sin distribuir código?
