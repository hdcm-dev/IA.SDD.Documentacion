**Comisión: Formal** (lógica, conjuntos cerrados, decidibilidad), postergada por cupo en el ciclo anterior (deuda D-5). Siglas: `M9-FOR-NN`.

**Tu competencia:** que los conjuntos que la regla declara cerrados lo sean, que los mapeos sean totales y funcionales, que las condiciones sean decidibles con la información disponible en el momento en que se evalúan, y que los criterios enumerables sean efectivamente enumerables. **No te corresponde** seguridad, trazabilidad documental ni la conveniencia del diseño.

**Tu única pregunta:** ¿las definiciones de `Expediente-Rules.md` son decidibles y totales? En particular:
1. **§3.2**: seis tipos y el mapeo tipo del último folio → estado. Enumeralo: ¿es total (todo folio deja exactamente un estado) y funcional (nunca dos estados)? ¿Qué pasa con `presentacion` cuando hay una `resolucion` anterior seguida de un `archivo`? ¿Con `constancia` con `Suspende hasta:` seguida de otra `constancia` sin la línea? ¿Con un `archivo` seguido de una `presentacion`? Construí los casos y decí si el estado queda definido.
2. **§1**: la condición de apertura (dos ramas) y las exclusiones. ¿Es decidible **al abrir** con lo que la presentación trae, o exige predecir? Construí un caso que caiga en las dos listas a la vez (abre y no abre) si existe. El ciclo anterior rechazó una condición por ser predicción (folio 014 R-02, folio 015 Q2 4-1).
3. **§6**: A1 a A10. Por cada uno: ¿el comando decide lo que el criterio enuncia, ni más ni menos? Corré `bash <scratchpad>/a1a10.sh <scratchpad>/caso SDD/Expedientes/00001-Enlace-Roto-En-Roadmap` y leé `<scratchpad>/ev/ev-03-a1a10-reales.out`. Buscá falsos positivos y falsos negativos: un folio con `Corrige | 0012` (cuatro dígitos), un tipo escrito `Presentacion`, un pase que no es la última línea porque hay una línea vacía al final, un testimonio con dos bloques, una carpeta con seis dígitos, un `README.md` con un campo repetido.
4. **A8**: la huella del bloque extraído con `awk` agrega un salto de línea final por línea; si el original no termina en salto de línea, la huella del bloque y la del archivo original difieren. ¿La regla lo declara? ¿Qué debería decir?
5. **Q11** en el plan (cambio 2, `Master-Prompt.md` §8.1): «ninguna detención de las dos familias que preguntan sale sin este orden» de cinco pasos. ¿El orden es una secuencia bien definida, o hay pasos que dependen de un resultado no definido (por ejemplo, «lo que sobrevive y cumple §0.0»)? ¿Los bloqueantes de §7.0 y los disparadores 2 y 3 de `Mesa-Rules.md` §7 quedan bien excluidos?

Contá los campos de la forma mínima sobre `<scratchpad>/caso/` con un comando y decí si son dieciocho.
